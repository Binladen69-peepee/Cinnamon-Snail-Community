import { LaterPhasePage } from "@/components/ui/later-phase";

export default function RoadmapPage() {
  return (
    <LaterPhasePage
      title="Your roadmap arrives in Phase 4B"
      body="Four answers will shape a track, not 128 separate plans. This page stays honest until that engine exists."
    />
  );
}
