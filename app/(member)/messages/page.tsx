import { LaterPhasePage } from "@/components/ui/later-phase";

export default function MessagesPage() {
  return (
    <LaterPhasePage
      title="Direct messages are Phase 4A"
      body="1:1 and small-group messaging with server-side permission checks will land after the social layer. The data tables already exist."
    />
  );
}
