import { LaterPhasePage } from "@/components/ui/later-phase";

export default function BulletinPage() {
  return (
    <LaterPhasePage
      title="Bulletin board is Phase 4F"
      body="Happenings, member services, and the map will live here. Home addresses will never be stored on profiles."
    />
  );
}
