import { LaterPhasePage } from "@/components/ui/later-phase";

export default function LearnPage() {
  return (
    <LaterPhasePage
      title="Courses are next"
      body="The campus will play lessons with entitlement checks, captions, and resume in Phase 3. Nothing here is mocked as if a course already exists."
    />
  );
}
