import { LaterPhasePage } from "@/components/ui/later-phase";

export default function CalendarPage() {
  return (
    <LaterPhasePage
      title="Events calendar comes with courses"
      body="Timezone-aware RSVPs, capacity, and reminders are scheduled for Phase 3."
    />
  );
}
